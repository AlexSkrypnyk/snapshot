{
    "name": "alexskrypnyk/snapshot",
    "description": "Directory snapshot, diff, and patch system useful for test fixtures.",
    "license": "GPL-2.0-or-later",
    "type": "library",
    "authors": [
        {
            "name": "Alex Skrypnyk",
            "email": "alex@drevops.com",
            "homepage": "https://alexskrypnyk.com",
            "role": "Maintainer"
        }
    ],
    "homepage": "https://github.com/alexskrypnyk/snapshot",
    "support": {
        "issues": "https://github.com/alexskrypnyk/snapshot/issues",
        "source": "https://github.com/alexskrypnyk/snapshot"
    },
    "require": {
        "php": ">=8.2",
        "alexskrypnyk/file": "^0.19.0",
        "sebastian/diff": "^6.0 || ^7.0"
    },
    "require-dev": {
        "alexskrypnyk/phpunit-helpers": "^0.16.0",
        "dealerdirect/phpcodesniffer-composer-installer": "^1",
        "drevops/phpcs-standard": "^0.7",
        "drupal/coder": "^9.0.1@alpha",
        "ergebnis/composer-normalize": "^2.52.0",
        "phpbench/phpbench": "^1.7.0",
        "phpstan/phpstan": "^2",
        "phpunit/phpunit": "^11.5.56",
        "rector/rector": "^2"
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "AlexSkrypnyk\\Snapshot\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "AlexSkrypnyk\\Snapshot\\Benchmarks\\": "benchmarks",
            "AlexSkrypnyk\\Snapshot\\Tests\\": "tests/phpunit"
        },
        "classmap": [
            "tests/phpunit"
        ]
    },
    "bin": [
        "bin/update-snapshots"
    ],
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "ergebnis/composer-normalize": true
        },
        "audit": {
            "abandoned": "report",
            "block-insecure": true
        },
        "discard-changes": true,
        "sort-packages": true
    },
    "scripts": {
        "benchmark": "mkdir -p .logs >/dev/null && phpbench run --ref=baseline --report=aggregate --dump-file .logs/performance-report.json --output html --output csv --output console",
        "benchmark-baseline": "mkdir -p .logs .phpbench >/dev/null && phpbench run --store --tag=baseline --report=aggregate --dump-file .logs/performance-report.json --output html --output csv --output console",
        "lint": [
            "phpcs",
            "phpstan",
            "rector --dry-run"
        ],
        "lint-fix": [
            "rector",
            "phpcbf"
        ],
        "reset": "rm -Rf vendor composer.lock",
        "test": "phpunit --no-coverage",
        "test-coverage": "phpunit"
    }
}
